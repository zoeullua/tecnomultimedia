boolean tocabotonA(int x, int y, int ancho, int alto){ // funcion con parametro que retorna valor. corrobora si seleccione el boton A
 return (mouseX>x && mouseX < ancho && mouseY > y && mouseY < alto);
}
