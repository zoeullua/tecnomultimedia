void funcioncentro(){ // funcion con parametro que no retorna valor
 rectMode(CENTER);
  // ciclo for ( iniciacion; condicion de corte; modificacion);
   for (int x = 70; x > 0; x-= 57){
     rect(600, 200, x, x);
}}
